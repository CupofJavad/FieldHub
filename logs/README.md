# TGND Logs

Runtime application and error logs are written here (or to stdout in containers). Log files are gitignored. See `docs/ERROR_LOGGING_SYSTEM.md` for the logging design. Versioned report exports may live under `logs/reports/` per the file versioning system.
