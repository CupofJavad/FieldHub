#!/usr/bin/env node
/**
 * TGND – OpenAPI to unit tests generator (scaffold).
 * Implement in Phase 1. See docs/UNIT_TESTING_GENERATION_SYSTEM.md.
 */
const path = require('path');

const repoRoot = path.resolve(__dirname, '../../../..');
const specPath = path.join(repoRoot, 'apps/api/openapi.yaml');

// Scaffold: no-op until Phase 1
console.log('OpenAPI-to-tests generator: scaffold (not yet implemented).');
console.log('Expected spec path:', specPath);
process.exit(0);
