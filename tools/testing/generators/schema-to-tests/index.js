#!/usr/bin/env node
/**
 * TGND – Schema to unit tests generator (scaffold).
 * Implement in Phase 1–2. See docs/UNIT_TESTING_GENERATION_SYSTEM.md.
 */
const path = require('path');

const repoRoot = path.resolve(__dirname, '../../../..');
const schemaPath = path.join(repoRoot, 'packages/canonical-model');

// Scaffold: no-op until Phase 1–2
console.log('Schema-to-tests generator: scaffold (not yet implemented).');
console.log('Expected schema/base path:', schemaPath);
process.exit(0);
